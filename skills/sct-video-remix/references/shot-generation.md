# 按真实镜头生成，不按功能段硬拆

用户要忠实复刻多镜头原片时使用。此路线复用每镜现有 `compile/run`，用独立 `video-remix-assembly.v1` 合同组装已下载片段；不修改旧计划/规格格式，不强制所有视频都分段。

## 从原片决定生成单位

核对实际切点：前后视角、主体或场景的突变才是候选切镜，闪光、镜片变暗、遮挡并不自动算切镜。一镜到底里的拿起、翻转、放下保留连续，不能按“外观/卖点/推荐”拆开。同款默认保留原镜头顺序、动作幅度、有效重复和节奏；只换人/场景时保持新场景提供相同支撑和拍摄条件。

每个镜头建立一份普通制作计划，时间从本镜0开始。只输入该镜需要的画面/商品图，不把全片所有状态图、人物和场景塞给每个任务；共同的人物、商品、穿搭、地点约束一致。必要的跨镜一致性用共同画面参考传递，但不能让上一镜错误的商品形态成为下一镜真源。生成图片仍须回查原始商品部件，合成图不能证明机构成立。

保存一份简短镜头清单：原片区间、生成版本ID、生成时长、最终取用区间、声音安排及共享素材。它用于制作和恢复，不需要用户逐镜审批。原片切点不能精确执行时，生成预留明确的静止/收尾余量，再选择实际可用边界；模型最短时长大于原镜时长时也保留额外尾部，不把整段变速塞回。接触、翻转和触发恢复的中间过程不可被裁掉来掩盖失败。无法在本轮预算内覆盖所有镜头时如实报告，不偷偷合并相异镜头来凑请求数。

## 声音不是每镜重新开口

先区分同步口播、跨镜旁白、无口播。同步口播以完整语句和实际唇形为约束，不能将另一段配音硬盖上去；一镜到底优先由同一生成任务完成。跨镜旁白作为一个连续表达单元安排，不因画面切镜重新起句。

组装支持三种明确选择：`clips` 保留每镜自带原声，但跨任务声线与语流一致性仍待验证；`continuous` 使用本项目指定已生成视频的一条连续音轨，画面取自分镜，注明音轨的原任务/区间；`silent` 明确去声。不能默认静音解决声音困难，也不能偷偷复用对标原声。已有生成音轨只有台词和时序仍符合本次目标才可复用；否则说明需独立声音制作及其授权，当前后端不提供配音或声纹克隆服务。局部现场声混音暂不支持，连续音轨替换会连同各段现场声一起替换，交付必须说明。

这使画面路线的对照可使用同一声音条件，但不证明声音问题已修复；最终仍检查动作触发与对应词句是否合拍。无口播也需要保留用户要求的音乐/现场声安排，不默认所有片段都有可拼音轨。

## 可恢复执行与组装

用现有 `compile` 分别登记计划，按已授权总预算 `run <镜头ID…>`；未完成时恢复原ID。镜头失败可在授权范围另建版本，仅替换组装清单对应项。原片、好镜头和上一版组装保持不变，不因一镜失败重生全片。

`assemble /absolute/assembly.json` 默认只读校验/预览；加 `--execute` 才原速硬切组装。示例仅解释字段：

```json
{
  "format": "video-remix-assembly.v1",
  "id": "cut-01",
  "clips": [
    {"variant": "shot-01", "in": 0, "out": 3.3},
    {"variant": "shot-02", "in": 0.2, "out": 4.4}
  ],
  "audio": {"mode": "continuous", "variant": "narration-01", "in": 0}
}
```

用真实已下载版本和实际可用区间替换示例。当前声音variant也须在本项目，不能伪造运行记录导入外项目音轨。输出在 `assemblies/<id>/`，保留合同、原文件哈希/任务ID、实际处理参数与结果；原 `variants/<id>/raw.mp4` 不变。新剪法或局部替换用新组装ID，失败记录不得抹掉。画幅/分辨率不同先停，不自动裁画面；统一30帧是标准重采样（可能重复/丢弃已有帧），不是运动插帧，不自动变速、加转场、字幕或调色。

## 验证的是完整展示，不只是接得上

先交付组装片、各段原片与实际条件。检查是否保留原镜头和动作覆盖、切点是否破坏中间过程、跨镜商品/人物是否变化，以及声音完整性。与整条生成比较时标明历史基线、共同/不同输入和声音来源；补图、换场景或声音后期同时变化属于组合路线试验，不称纯粹“分段”的单变量实验。不因局部变形少了就忽略漏展示、声音漂移或总成本增加。

该入口只做明确片段的组装，不自动发现切点、追踪人物、对齐词级时间或保证连续长镜头无缝续接。一次样片仅验证这组条件，不代表所有品类成功率已提高。
