[project]
name = "video-remix"
version = "0.3.3"
description = "Reference-led video remix and variation tools for AI assistants."
requires-python = ">=3.10"
dependencies = []

[project.scripts]
video-remix = "video_remix.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["video_remix"]
