# Video Remix

面向 AI 助手的视频复刻与裂变工具箱。优先无口播 Before/After；真实账号执行，无零密钥演示。

- Skill 负责理解、提示词和裂变；Python 标准库后端负责素材、任务、调用、恢复和更新。
- 用户项目目录与程序版本分离。不要提交个人媒体、任务日志、配置或凭据。
- 已提交任务只能按原任务 ID 查询、下载；不确定是否提交成功时停下核实，绝不自动补交。
- 生成前显示输入、版本数和预算；沿用用户已给的授权，不重复增加审批层。
- 只对已验证的平台行为做承诺。离线适配器测试不冒充真实生成效果。
- 更新使用新版本目录和原子指针切换，保留回退。不要改写运行中的版本。
- 修改后运行 `python3 -m unittest discover -s tests -v` 和 `python3 -m video_remix doctor`。
